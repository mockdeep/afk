require 'spec_helper'

describe AFK do
  it 'has a version number' do
    expect(AFK::VERSION).not_to be nil
  end

  it 'does something useful' do
    var = false
    expect(var).to eq(true)
  end
end
